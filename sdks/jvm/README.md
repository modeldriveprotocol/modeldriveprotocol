# JVM SDKs

This directory contains the JVM client SDKs for Model Drive Protocol:

- `java-client`: the core Java runtime client
- `kotlin-client`: Kotlin coroutine and DSL wrappers on top of the Java client
