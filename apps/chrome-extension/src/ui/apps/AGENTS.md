# UI Apps Agents

- Keep `options` and `sidepanel` grouped here as the two extension UI applications.
- Put cross-application helpers in sibling directories such as `foundation`, `platform`, `market`, `i18n`, or `workspace-bundle` instead of duplicating them here.
- Read `options/AGENTS.md` or `sidepanel/AGENTS.md` for surface-specific guidance.
