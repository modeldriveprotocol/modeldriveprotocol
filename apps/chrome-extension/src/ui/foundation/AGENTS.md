# Foundation Agents

- Keep this directory limited to cross-surface UI primitives: appearance providers, theme definition, icon helpers, and global CSS.
- Do not put Chrome runtime messaging, storage access, or feature-specific state here.
- If a helper is only used by `options` or `sidepanel`, keep it in that feature directory instead of promoting it here.
