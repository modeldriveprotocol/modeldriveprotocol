# Platform Agents

- Keep this directory focused on UI-to-Chrome adapters such as runtime messaging, options navigation, sidepanel opening, and config load/save helpers.
- Do not add React rendering or MUI concerns here.
- Shared payload shaping is acceptable here when it exists only to hide Chrome API details from UI surfaces.
